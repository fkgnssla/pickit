image: 8.10.1-jdk21

stages:
  - build

cache:
  paths:
    - .gradle/wrapper/
    - .gradle/caches/

build_Core:
  stage: build
  script:
    - cd /home/ubuntu/PickIt/S11P21A309/Backend/Core
    - chmod +x ./gradlew
    - ./gradlew clean build -x test
    - sh ~/spring.sh
    - sh ~/contract.sh # 임시
  only:
    - release
  tags:
    - backend

